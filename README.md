# Auditoría 5S y Satisfacción - Sistema de Mejora Continua

## Tecnologías
- **Backend**: FastAPI + SQLAlchemy + SQLite
- **Frontend**: React + Vite + TailwindCSS
- **Auth**: JWT + bcrypt

## Instalación

### Backend
```bash
cd backend
python -m venv venv
.\venv\Scripts\Activate
pip install -r requirements.txt
uvicorn main:app --reload
